//first, lets make sure the calculator outputs '0' in the beginning

let output = 0; //define a variable which will be updated as buttons are pressed.
const display = document.getElementById('display-container').innerHTML; //this will make it quicker to reassign each time
display = output;



